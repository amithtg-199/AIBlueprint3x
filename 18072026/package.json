{
  "name": "qa-mentor",
  "version": "1.0.0",
  "description": "Cross-platform runner for QA Mentor",
  "scripts": {
    "start": "concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "start:frontend": "npm run dev --prefix frontend",
    "start:backend": "cd backend && (uv run uvicorn main:app --reload --port 8000 || ~/.local/bin/uv run uvicorn main:app --reload --port 8000)"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
