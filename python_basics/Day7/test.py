# Tv = {'BreakingBad':100, 'GameOfThrones':1292, 'TMKUC' : 88}

# Keymax = max(zip(Tv.values(), Tv.keys()))[0]
# print(Keymax)

opers_symobol = ["+","-","*","/"]
for symbol in opers_symobol:
    print(symbol)
select_opers = input("Pick an Operation from above:")